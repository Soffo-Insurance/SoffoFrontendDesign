# Canela font (web)

Place your **Canela** webfont files here so the app can use them.

- Required: `Canela-Regular.woff2` (or update the path in `src/index.css` to match your file names)
- Canela is from [Commercial Type](https://commercialtype.com/catalog/canela). You need a web license to use the font files.
